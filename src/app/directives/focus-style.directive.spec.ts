import { FocusStyleDirective } from './focus-style.directive';

describe('FocusStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusStyleDirective();
    expect(directive).toBeTruthy();
  });
});
